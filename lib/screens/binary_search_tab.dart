import 'package:flutter/material.dart';

class BinarySearchTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Text(
      'Home',
      style: TextStyle(color: Colors.red),
    );
  }
}
